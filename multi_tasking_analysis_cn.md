# ESP32 称重主控器：多任务架构深度分析

## 1. 现有架构局限性
系统目前采用单线程异步循环模型（Event Loop）。虽然 `ModbusMaster` 本身是非阻塞的，但在实际运行中，CPU 资源依然由 UI 渲染、组合逻辑计算及通讯协议栈共享。

### 实时性约束：
- **Modbus (RS485)**：对 UART 中断处理及轮询精度有较高要求，任何延迟都可能导致后续设备响应堆积。
- **OLED UI**：每次刷新 128x64 屏幕（通过 I2C）需要占据约 20-40ms 的 CPU 时间，这在单核环境下会造成 Modbus 轮询的周期性抖动。
- **旋转编码器**：虽然采用 ISR（中断服务程序）处理，但在高 CPU 负载下，任务级响应可能出现延迟。

---

## 2. 双核 FreeRTOS 多任务方案评估

### 收益 (+)
1. **彻底并行化**：我们可以将 **核心控制逻辑与 Modbus 通讯 (Core 1)** 与 **HMI 渲染 (Core 0)** 完全隔离。屏幕刷新的阻塞将不再干扰 Modbus 报文处理。
2. **确定性响应**：关键控制任务（如伺服电机位置更新）可以赋予最高优先级，确保实时性。
3. **流畅的交互体验**：HMI 可以稳定维持在 30FPS 以上，不会受 RS485 超时或复杂的组合查找逻辑影响。

### 风险与成本 (-)
1. **竞态条件 (Race Conditions)**：共享变量（如量程参数 `targetMin/Max`、缓存重量 `cachedWeights`）必须通过 **互斥锁 (Mutex)** 或 **信号量 (Semaphore)** 保护。
2. **内存开销**：每个任务都需要独立堆栈（通常需 4KB 以上），会增加 SRAM 消耗。
3. **自调试难度**：多任务环境下的堆栈溢出或死锁检测比单线程复杂。

---

## 3. 实施方案建议 (双核并行)

| 任务名称 | 运行核心 | 优先级 | 职责描述 |
| :--- | :---: | :---: | :--- |
| **ControlTask** | 1 | 10 (高) | Modbus 轮询、称重组合引擎、伺服电机控制。 |
| **UITask** | 0 | 5 (中) | OLED 渲染、菜单导航、参数编辑逻辑。 |
| **SystemTask** | 0 | 1 (低) | NVS 指令固化、串口波图、系统诊断。 |

### 数据同步策略：
- **配置参数**：使用 `SemaphoreHandle_t` 互斥锁确保 UI 修改参数时，控制逻辑不会读到中间态。
- **重量数据**：采用“快照”模式，或通过轻量级原子操作同步 32 位浮动重量数据。

---

## 4. 关于 Modbus 扫描故障的深度排查
在转向多任务架构的同时，我们将针对 N81 物理配置进行底层验证：
- **消除显示干扰**：目前的扫描失败极大概率是因为 I2C 绘制进度条的过程阻塞了 UART 的及时响应。隔离到 Core 1 后，由于中断优先级更高且无 I/O 阻塞，扫描成功率将显著提升。

---
*冯氏称重系统 (FENGS WEIGH SCALE) 技术部*
